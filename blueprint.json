{
  "$schema": "https://playground.wordpress.net/blueprint-schema.json",
  "meta": {
    "title": "Category AJAX Filter (Free) — Legacy + Builder Demos",
    "description": "Installs CAF free, seeds legacy and builder filters with category checkbox terms, and opens the demo hub.",
    "author": "Trusty Plugins"
  },
  "landingPage": "/caf-playground/",
  "login": true,
  "preferredVersions": {
    "php": "8.2",
    "wp": "latest"
  },
  "features": {
    "networking": true
  },
  "steps": [
    {
      "step": "login",
      "username": "admin",
      "password": "password"
    },
    {
      "step": "setSiteOptions",
      "options": {
        "blogname": "CAF Free Playground",
        "blogdescription": "Legacy + builder filter demos with category terms",
        "permalink_structure": "/%postname%/"
      }
    },
    {
      "step": "installPlugin",
      "pluginData": {
        "resource": "git:directory",
        "url": "https://github.com/trustyplugins/category-ajax-filter",
        "ref": "playground",
        "refType": "branch"
      },
      "options": {
        "activate": true,
        "targetFolderName": "category-ajax-filter"
      }
    },
    {
      "step": "runPHP",
      "code": "<?php define('WP_DEBUG', true); define('WP_DEBUG_DISPLAY', true); @ini_set('display_errors', '1'); @ini_set('memory_limit', '512M'); require_once '/wordpress/wp-load.php'; $seed = WP_PLUGIN_DIR . '/category-ajax-filter/includes/playground/seed-demo.php'; if ( ! file_exists( $seed ) ) { throw new Exception( 'CAF seed file missing at ' . $seed ); } require_once $seed; $result = caf_playground_seed_demo(); if ( ! is_array( $result ) || ! empty( $result['error'] ) ) { $message = is_array( $result ) && ! empty( $result['error'] ) ? (string) $result['error'] : 'CAF seed failed.'; if ( ! empty( $result['file'] ) ) { $message .= ' in ' . $result['file']; } if ( ! empty( $result['line'] ) ) { $message .= ':' . $result['line']; } throw new Exception( $message ); } ?>"
    }
  ]
}
