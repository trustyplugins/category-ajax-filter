import React from "react";

/** Shared help copy for global / custom font settings (builder + plugin settings). */
export const globalFontFamilyTooltipContent = (
  <>
    Applies to new modules and updates existing modules still using the previous
    global font. Per-module font settings remain as overrides.
    <br />
    <br />
    Upload custom TTF fonts from Plugin Settings on the filters home screen.
    They appear here under Custom Fonts for this layout.
  </>
);
